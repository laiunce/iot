server:
  port: 8080

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/trgr?useSSL=true

management:
  endpoints:
    web:
      exposure:
        include: "*"
